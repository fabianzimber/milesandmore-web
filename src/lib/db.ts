/**
 * Database access for Next.js API routes.
 * Since all data is now in Redis and accessed via the Bot API,
 * this module is no longer needed for direct DB access.
 * All data fetching goes through the botApi.ts module instead.
 *
 * This file is kept as a placeholder in case direct Redis access
 * from Vercel is needed in the future.
 */

export {};
